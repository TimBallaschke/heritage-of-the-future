title: Room

columns:
  - width: 1/3
    fields:
      background_color:
        label: Background color
        type: color
        translate: false
      image_color:
        label: Image color
        type: color
        translate: false
      font_color:
        label: Font color
        type: color
        translate: false
  - width: 2/3
    sections:
      artworks:
        type: pages
        label: Artworks
        template: artwork
        info: "#{{ page.num }} — {{ page.artist_name }}"
